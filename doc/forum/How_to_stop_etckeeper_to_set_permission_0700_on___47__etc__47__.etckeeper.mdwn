Etckeeper continuously resets the file permission for `/etc/.etckeeper` back to `0700`. For backup reasons I need it to be group-readable at least, i.e. `0740`. Every time etckeeper resets the file permissions, my nightly backup breaks.

How do I keep etckeeper from resetting the file permissions?
